# cloudflared yapılandırması — bu dosyayı .env olarak kopyalayıp düzenleyin.
# cli.sh start üç moddan birini seçer (öncelik sırasıyla):
#
#   1) TUNNEL_TOKEN dolu          -> named tunnel, token ile (dashboard'dan alınır)
#   2) config.yml dosyası var     -> named tunnel, yerel config ile
#   3) hiçbiri yok                -> quick tunnel (*.trycloudflare.com), LOCAL_URL'e bakar

# Cloudflare Zero Trust > Networks > Tunnels ekranından alınan token
TUNNEL_TOKEN=

# config.yml ile çalışırken opsiyonel tünel adı/UUID'si
TUNNEL_NAME=

# Quick tunnel modunda yayınlanacak yerel adres
LOCAL_URL=http://localhost:8080
